export enum SortOrder {
	ASC = 'ASC',
	DESC = 'DESC',
}

export enum PaginationOrder {
	ASC = 'ASC',
	DESC = 'DESC',
}

export enum Status {
	ACTIVE = 'ACTIVE',
	INACTIVE = 'INACTIVE',
	DELETED = 'DELETED',
}

export enum Gender {
	MALE = 'MALE',
	FEMALE = 'FEMALE',
	OTHER = 'OTHER',
}

export enum Direction {
	ASC = 1,
	DESC = -1,
}